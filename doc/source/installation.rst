============
Installation
============

At the command line::

    $ pip install datadog-logging

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv datadog-logging
    $ pip install datadog-logging
